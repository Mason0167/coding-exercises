package src.Lab_1;

public class MainLab1 {
    public static void main(String[] args) {
        /*
        // Tip press < shortcut actionId ="ShowIntentionActions"/> with your caret at the highl
        // to see how Intellij IDEA suggests fixing it
        // the program start here!

        //IPO ---> Input ---> process ---> output
        //Input and Output are in front of string

        int a = 5;
        int b = 10;
        double c = 5.6;
        boolean d = true;

        //These two are the same
        String name = "abc";
        //an Object
        String n = new String("abc");

        System.out.println("The result is " + a + b);
        System.out.println("The result is " + (a + b));
        System.out.println("The result is " + a++ + ++b);
        // a++ : use value a first and then increment it
        // ++b : increment b first and use it

        System.out.println("The result is " + (a++ + ++b));
        System.out.println(a);

        System.out.printf("The numbers are %d, %d, %.2f\n",  a, b, c);

        //===========================================================
        //I want to collect someone full name!


        System.out.println("Enter your first name and last name: ");
        // In this line, you can enter your full name and integers at the same time to skip all the nextInt()

        String fname = sc.next();
        String lname = sc.next();

        System.out.println("Your full name is: " + fname + " " + lname);

        System.out.println("Enter the first integers: ");
        // Each variable has a dedicated class for all the methods you may need
        int n1 =  Integer.parseInt(sc.nextLine());
        System.out.println("Enter the second and third integers: ");
        int n2 =  sc.nextInt();
        int n3 =  sc.nextInt();
        System.out.println("The sum is:" + (n1 + n2 + n3));

         */

        //========================================================================
        // Collect three integers find the sum and avg of those 3 and
        // Show the biggest value contain in any of those numbers
/*
        Scanner sc = new Scanner(System.in);

        System.out.println("Enter the first integers: ");
        int n1 =  sc.nextInt();
        int n2 =  sc.nextInt();
        int n3 =  sc.nextInt();

        // Method 1
        int sum = n1 + n2 + n3;
        double avg = sum / 3.0;
        System.out.printf("The sum is: %d and The avg is: %.2f \n ", sum, avg);


        //Method 2
        System.out.println("The sum is:" + (n1 + n2 + n3));
        System.out.println("The avg is:" + (n1 + n2 + n3)/3);
        int max = Math.max(n1, Math.max(n2, n3));
        System.out.println(max);
        */

        System.out.println(1200000000000L);

    }
}